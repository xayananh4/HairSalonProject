@{
  Layout = "_Layout";
}

@using HairSalon.Models;

<h1>Clients</h1>

@if (@Model.Count == 0)
{
  <h3>You have not added any clients yet!</h3>
} 

@foreach (Client element in Model)
{
  <li>@element.Name</li>
}
